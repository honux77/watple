# need to uncomment bind option /etc/mongdodb.conf
# need to enable authentication option

use admin
db.dropuser('honux')
db.createUser(
  {
    user: "honux",
    pwd: "pass123$",
    roles: [ { role: "userAdminAnyDatabase", db: "admin" } ]
  }
)
use webdb
db.dropUser('webuser')
db.createUser(
  {
    user: "webuser",
    pwd: "pass567$",
    roles: [ { role: "userAdmin", db: "webdb" },
            { role: "readWrite", db: "webdb" },
        ]
  }
)
