# need to uncomment bind option /etc/mongdodb.conf
# need to enable authentication option

use admin
db.dropuser('wadmin')
db.createUser(
  {
    user: "wadmin",
    pwd: "pass123$",
    roles: [ { role: "userAdminAnyDatabase", db: "admin" } ]
  }
)
use watplewebdb
db.dropUser('watpleweb')
db.createUser(
  {
    user: "watpleweb",
    pwd: "pass567$",
    roles: [ { role: "userAdmin", db: "watplewebdb" },
            { role: "readWrite", db: "watplewebdb" },
        ]
  }
)
